{
{% for host in groups["all"] %}
  {% if  host not in ansible_facts["hostname"] %}

  "{{ host }}": {
    "visible": true,
    "address": "{{ host }}"
  }{% if not loop.last %},{% endif %}

  {% endif  %}
{% endfor %}
}
